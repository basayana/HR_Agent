Which model to pick?
make sure you set GROQ_API_KEY env variable.
run in PS: (Invoke-WebRequest -Uri "https://api.groq.com/openai/v1/models" -Headers @{ Authorization = "Bearer $env:GROQ_API_KEY" } -UseBasicParsing | ConvertFrom-Json).data.id
check the model names, should match with what we use in hr_agent.py